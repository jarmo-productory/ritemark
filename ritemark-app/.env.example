# RiteMark Environment Variables Configuration
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: Never commit .env.local to git (it's in .gitignore)

# ===================================================================
# Google OAuth Configuration (Required)
# ===================================================================

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/apis/credentials
# Sprint 20: Used for both backend and browser-only OAuth flows
VITE_GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# ===================================================================
# OAuth Configuration (Optional)
# ===================================================================

# OAuth Redirect URI (for local development)
# Default: http://localhost:5173
# Only change if using a different port
VITE_OAUTH_REDIRECT_URI=http://localhost:5173

# Use Mock OAuth (for development without Google setup)
# Set to "true" to bypass real OAuth during development
# Default: false
VITE_USE_MOCK_OAUTH=false

# ===================================================================
# OpenAI Configuration (Optional - For AI Features)
# ===================================================================

# OpenAI API Key (Bring Your Own Key - BYOK)
# Get this from: https://platform.openai.com/api-keys
# Sprint 23: Used for AI-powered text editing commands
# Example: sk-proj-...
VITE_OPENAI_API_KEY=your-openai-api-key-here

# ===================================================================
# Feature Flags (Optional)
# ===================================================================

# Enable Sidebar Scaffold (UI development mode)
# Shows sidebar component structure and navigation
# Default: false (production), true (development)
VITE_USE_SIDEBAR_SCAFFOLD=false

# ===================================================================
# Backend Configuration (Netlify Functions Only - DO NOT SET IN BROWSER)
# ===================================================================

# These variables should ONLY be set in Netlify Dashboard or .env file
# at repository root (for backend Netlify Functions)
# NEVER set these with VITE_ prefix (they should NOT be exposed to browser)

# GOOGLE_CLIENT_SECRET=your-client-secret-here
# FRONTEND_URL=http://localhost:8888

# ===================================================================
# Getting Your Google OAuth Credentials
# ===================================================================

# 1. Go to Google Cloud Console: https://console.cloud.google.com
# 2. Create a new project or select existing project
# 3. Enable Google Drive API and Google People API
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client ID"
# 5. Application type: "Web application"
# 6. Authorized JavaScript origins:
#    - http://localhost:5173 (local dev)
#    - https://ritemark.netlify.app (production)
# 7. Authorized redirect URIs:
#    - http://localhost:8888/.netlify/functions/auth-callback (local backend)
#    - https://ritemark.netlify.app/.netlify/functions/auth-callback (production backend)
# 8. Copy the Client ID to VITE_GOOGLE_CLIENT_ID above
# 9. Copy the Client Secret to Netlify Dashboard environment variables

# ===================================================================
# Security Notes
# ===================================================================

# ⚠️ Shell Environment Variables Override .env.local
#    If you see "client_id=your-client-id-here" in OAuth popup:
#    1. Run: env | grep VITE_GOOGLE_CLIENT_ID
#    2. If set in shell, unset it or start dev server with explicit override:
#       VITE_GOOGLE_CLIENT_ID=actual-value npm run dev

# ✅ Always use .env.local for local development (never .env)
# ✅ Never commit .env.local to git
# ✅ Never set VITE_* variables in shell environment
# ✅ Use Netlify Dashboard for backend secrets (GOOGLE_CLIENT_SECRET)
